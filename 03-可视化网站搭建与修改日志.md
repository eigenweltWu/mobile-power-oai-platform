# OAI 可视化与在线控制网站搭建日志

> 日期：2026-08-13  
> 网站目录：`/home/usrp2/桌面/OAI(1)/oai-control-center`  
> 范围：前端、本地 API、Web SoftScope、X310 采样、网速测试、日志与局域网数据访问。

## 1. 当前服务地址

| 服务 | 地址 | 暴露范围 |
| --- | --- | --- |
| 前端页面 | `http://127.0.0.1:3000` | 本机 |
| 数据/控制 API | `http://0.0.0.0:8787` | 本机所有网卡，可从 LAN 访问 |
| OAI WebScope | `127.0.0.1:8090` | 主要供本机页面使用 |

局域网应用可使用本机任一可路由网卡 IP，例如当前 Wi-Fi：

```text
http://192.168.31.119:8787/api/health
http://192.168.31.119:8787/api/status
http://192.168.31.119:8787/api/logs?lines=180
```

另有实验网地址 `192.168.40.3`、`192.168.100.19`、`192.168.100.10`，但客户端应选择与自身同网段且可路由的地址。

## 2. 技术栈与文件

- React 19 + TypeScript。
- vinext/Vite 负责开发和构建。
- Python 3 `ThreadingHTTPServer` 实现本地 API。
- Canvas 绘制实时趋势、瀑布、IQ、星座和 LLR。
- OAI WebScope REST + WebSocket 提供 PHY 二进制数据。

| 文件 | 作用 |
| --- | --- |
| `app/page.tsx` | 主页、gNB 操作、射频参数、UE、测速、日志 |
| `app/ScopePanel.tsx` | Scope 描述、WebSocket 解析和 PHY 图形 |
| `app/globals.css` | 白色主题和响应式布局 |
| `backend/server.py` | Docker 控制、配置持久化、指标解析、X310 采样、测速、LAN API |
| `start-dashboard.sh` | 启动 API 和前端 |
| `tests/rendered-html.test.mjs` | 渲染和功能回归测试 |

## 3. 启动

```bash
cd '/home/usrp2/桌面/OAI(1)/oai-control-center'
./start-dashboard.sh
```

`start-dashboard.sh` 现默认执行：

```bash
python3 backend/server.py --host 0.0.0.0
npm run dev -- --host 127.0.0.1
```

可用以下方式恢复 API 仅本机监听：

```bash
OAI_API_HOST=127.0.0.1 ./start-dashboard.sh
```

## 4. 局域网 API 暴露与安全边界

### 4.1 本次修改

- Python 后端默认 host 从 `127.0.0.1` 改为 `0.0.0.0`。
- 启动脚本增加 `OAI_API_HOST`，默认 `0.0.0.0`。
- CORS 保持开启，其他应用可跨域 GET JSON。
- 增加远程写保护：默认只允许 loopback 客户端 POST，局域网客户端只读。

### 4.2 默认权限

| 请求 | 本机 | 局域网 |
| --- | --- | --- |
| GET `/api/health` | 允许 | 允许 |
| GET `/api/status` | 允许 | 允许 |
| GET `/api/logs` | 允许 | 允许 |
| POST gNB/增益/频率/测速 | 允许 | 默认 HTTP 403 |

如果明确需要受信任 LAN 中的其他应用远程开关 gNB 或修改射频参数，可显式开启：

```bash
OAI_LAN_CONTROL=1 ./start-dashboard.sh
```

该模式没有内置身份验证，不应在校园公共网、不受信任 Wi-Fi 或公网中开启。若需生产级远程写控制，应增加反向代理、TLS、API token/mTLS 和防火墙白名单。

## 5. API 清单

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | `/api/health` | 健康检查 |
| GET | `/api/status` | gNB/5GC、射频、UE、X310、Scope、测速 |
| GET | `/api/logs?lines=180` | gNB 和核心网日志 |
| POST | `/api/gnb/service` | `start` / `stop` / `restart` |
| POST | `/api/gnb/gains` | 修改 TX/RX gain 并重启 |
| POST | `/api/gnb/frequency` | 修改频率、对齐栅格并重启 |
| POST | `/api/x310/service` | `start`/`stop` X310 RF1 UHD 周期采集；不对 X310 断电 |
| POST | `/api/nettest` | 启动/停止上行或下行测速 |

### 5.1 GET `/api/health` 响应

```json
{
  "ok": true
}
```

`ok=true` 仅表示 API 进程正常响应，不表示 gNB、核心网或 USRP 一定正常。

### 5.2 GET `/api/status` 响应

以下为缩略快照。实际响应中 `core.services` 含全部受管容器，`iq.points` 通常含数百个点。

```json
{
  "timestamp": "2026-08-13T13:29:24.673144+00:00",
  "gnb": {
    "name": "oai-gnb-runner",
    "running": true,
    "status": "running",
    "startedAt": "2026-08-13T12:51:58.690127054Z"
  },
  "core": {
    "running": 10,
    "total": 10,
    "services": [
      { "name": "oai-amf", "running": true, "status": "running", "startedAt": "2026-08-13T09:20:42.72776707Z" }
    ]
  },
  "radio": {
    "band": 78,
    "arfcn": 630624,
    "pointA": 629352,
    "frequencyMHz": 3459.36,
    "bandwidthMHz": 40,
    "txGainDb": 60.0,
    "rxGainDb": 40.0,
    "txAmplitudeDbfs": -36.0,
    "monitor": {
      "enabled": true,
      "available": true,
      "rmsDbfs": -25.52,
      "peakDbfs": -2.25,
      "error": "",
      "ageSeconds": 0.6
    }
  },
  "ues": [
    {
      "rnti": "8d6b",
      "cuId": 1,
      "state": "in-sync",
      "rsrp": -65,
      "snr": 22.0,
      "dlMbps": 0.0,
      "ulMbps": 0.002,
      "dlMcs": 0,
      "ulMcs": 0,
      "dlBler": 0.00065,
      "ulBler": 0.07406,
      "imsi": "466920000000001"
    }
  ],
  "iq": {
    "available": true,
    "source": "X310 192.168.40.2 RF1 RX",
    "points": [{ "i": 0.049, "q": 0.0199 }]
  },
  "hardware": {
    "x410": { "present": true, "required": true, "role": "5G gNB", "address": "192.168.100.2", "txPort": "RF0 TX/RX0", "rxPort": "RF0 RX1", "rxStreams": 1 },
    "x310": { "enabled": true, "present": true, "required": false, "role": "可移除 RF 测量终端", "address": "192.168.40.2", "rxPort": "RF1 RX", "error": "" },
    "scope": { "nativeBuilt": true, "webBuilt": true, "active": true, "supports": ["X410 RX 时域波形", "PUSCH 均衡后 I/Q 星座", "PUSCH LLR", "上行信道估计"] }
  },
  "communication": {
    "nettest": { "running": false, "direction": "downlink", "rateMbps": 0.0, "averageMbps": 0.0, "bytes": 0, "elapsedSeconds": 0.0, "ueIp": "", "serial": "", "error": "", "limitSeconds": 300 }
  }
}
```

主要字段语义：

| 字段组 | 说明 |
| --- | --- |
| `timestamp` | ISO 8601 UTC 快照生成时间。 |
| `gnb` | gNB Docker 容器的名称、运行值、Docker status 和启动时间。 |
| `core.running/total` | 正在运行的 5GC/IMS 服务数/受管总数。 |
| `core.services[]` | 各容器的 `name`、`running`、`status`、`startedAt`。 |
| `radio.band` | NR band 号。 |
| `radio.arfcn/pointA` | SSB 和 Point A NR-ARFCN。 |
| `radio.frequencyMHz/bandwidthMHz` | 当前中心频率与标称带宽，单位 MHz。 |
| `radio.txGainDb/rxGainDb` | 从 OAI 配置换算的 X410 增益，单位 dB。 |
| `radio.txAmplitudeDbfs` | gNB 启动日志的 TX_AMP，单位 dBFS；无法解析时为 `null`。 |
| `radio.monitor` | X310 是否可用、RMS/峰值 dBFS、错误文本和样本年龄秒数。 |
| `ues[]` | 仅包含 `in-sync` UE；无 UE 时为 `[]`。 |
| `ues[].rsrp/snr` | RSRP 单位 dBm，PUSCH SNR 单位 dB；尚无数据时可为 `null`。 |
| `ues[].dlMbps/ulMbps` | 由 MAC 累计字节差分得到的瞬时速率。 |
| `ues[].dlMcs/ulMcs` | 最近 DL/UL MCS index，可为 `null`。 |
| `ues[].dlBler/ulBler` | 0–1 比例，不是百分数；`0.07406` 约为 7.406%。 |
| `ues[].imsi` | AMF 映射到的 15 位 IMSI；尚未映射时该 key 可缺省。 |
| `iq.points[]` | X310 归一化 `i/q` 相对值，不是伏特；不可用时为 `[]`。 |
| `hardware.x410.present` | 表示 gNB 容器正在运行，不是独立 UHD 硬件探测。 |
| `hardware.x410.rxStreams` | OAI 实际启用的 RX stream 数，不是接线数。 |
| `hardware.x310.present` | 最近 X310 UHD 采样是否成功。 |
| `hardware.x310.enabled` | X310 UHD 周期采集开关；不表示硬件上电状态。 |
| `hardware.scope.active` | gNB 运行、已加 `--websrv` 且 8090 可达。 |
| `communication.nettest` | 测速是否运行、方向、瞬时/平均 Mbps、字节、时间、UE IP/ADB serial、错误和 300 秒上限。 |

### 5.3 GET `/api/logs` 响应

`lines` 默认为 180，有效范围 20–500；超界时会被截到最近边界。

```json
{
  "gnb": [
    "UE 8d6b: LCID 4: TX 28064660 RX 855546 bytes",
    "[NR_MAC] Frame.Slot 640.0",
    "UE RNTI 8d6b CU-UE-ID 1 in-sync PH 48 dB PCMAX 21 dBm, average RSRP -65 (16 meas)"
  ],
  "core": [
    "[oai-nrf] [nrf_app] [debug] Set NF status to REGISTERED",
    "[oai-nrf] [nrf_app] [info]",
    "[oai-nrf] Handle heartbeat timeout (NF update)"
  ]
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `gnb` | array<string> | `oai-gnb-runner` 最近日志行，已清理 ANSI 颜色字符。 |
| `core` | array<string> | 5GC/IMS 容器聚合日志，每行以 `[container-name]` 开头。 |

三个 GET 接口均返回 JSON、`Cache-Control: no-store`和 `Access-Control-Allow-Origin: *`。未知路径返回 HTTP 404 + `{"error":"not found"}`；内部读取错误返回 HTTP 500 + `{"error":"..."}`。更完整的逐字段类型和空值语义记录在 `oai-control-center/README.md`。

## 6. gNB 服务和射频控制

- 可启动、停止、重启 `oai-gnb-runner`。
- TX/RX gain 范围 0–60 dB。
- n78 40 MHz 载波中心频率范围 3319.68–3780.48 MHz。
- 频率按 1.44 MHz SSB 同步栅格对齐：

```text
SSB ARFCN = 600000 + raster_index × 96
Point A   = SSB ARFCN - 1272
```

- 修改前检查范围，首次操作建立 `.dashboard.bak`。
- 使用临时文件、`fsync` 和原子 `os.replace` 写入，成功后重启 gNB。

## 7. UE 与速率数据

从 gNB/AMF 日志解析：

- RNTI、CU-UE-ID、in-sync 状态和 IMSI 映射。
- RSRP、PUSCH SNR。
- DL/UL MCS 和 BLER。
- MAC TX/RX 累计字节与样本差分得到的实时 Mbps。

修复了旧 UE 假在线：遇到 `Remove NR rnti` 后删除速率基线，API 只返回 `in-sync` UE。

SNR 图增加 0–50 dB 纵轴刻度，不再仅显示弱/良好/优秀文字。

## 8. X310 外置测量

- X310 `192.168.40.2` RF1 RX 作为可移除测量端。
- “发射 / 接收幅度”面板增加“开启/停止 X310 采集”按钮。它控制后端 UHD 采样循环，不对 X310 断电，也不影响 X410/gNB。
- 停止后 RMS/峰值/IQ 置空，清空页面幅度历史，并显示“采集已停止”；启动后等待下一次采样恢复。
- UHD 每次采集 32768 个 sc16 样点，2 MS/s，RX gain 20 dB，约每 4 秒一次。
- 显示 RMS dBFS、peak dBFS 和归一化 IQ 散点。
- X310 IQ 是原始 RF 散点，不是 gNB 解调后 PUSCH 星座。
- X310 移除后该面板显示离线，不影响 gNB 和 UE 指标。

## 9. Web SoftScope 直接集成

原先曾嵌入 OAI 原生 Scope 页面，后改为直接解析数据：

1. GET `/oaisoftmodem/scopectrl/` 读图表描述。
2. 连接 `/softscope` WebSocket，以 `ArrayBuffer` 接收。
3. 解析 8-byte header 与 IQ=`1`、LLR=`2`、waterfall=`3`、response=`4`。
4. 订阅 Received Signal、SRS、PUSCH IQ 和 PUSCH LLR。
5. refresh rate 设为 `10`，TargetSelect `0`。
6. gNB 启动 12 秒后再连接，断线 3 秒后自动重连。
7. 删除 iframe，在统一白色 UI 中绘图。

四个图形：RF0 时域功率瀑布、SRS 频域响应、PUSCH 均衡后 IQ、PUSCH LLR。

## 10. IQ/LLR、自适应量程和频轴

- PUSCH IQ/LLR 只在 UE 有上行 PUSCH 时产生。无数据时显示原因，并可启动上行激励。
- IQ 使用 little-endian int16 I/Q 解析，LLR 按 OAI 值/增量对解析。
- 普通曲线 Y 轴用 P1–P99 + 6% padding。
- LLR 使用 |value| P99 对称量程。
- IQ 使用 `max(|I|,|Q|)` P99 × 1.12。
- 星座显示 I/Q min/max/RMS，LLR 显示数值和 bit index 范围。

SRS 频域：

- 有效带宽 106 × 12 × 30 kHz = 38.16 MHz。
- 显示带宽为 44 MHz，两侧留出带外，信号不贴图边。
- 左/中/右标注实际 MHz，虚线标记有效信号边缘。
- 以当前 3459.36 MHz 为例：显示 3437.36–3481.36 MHz，有效 3440.28–3478.44 MHz，两侧各约 2.92 MHz 带外。

## 11. 白色主题

- 背景、卡片和 Scope 画布改为白色。
- 调整边框、表格、日志、状态色和字体对比度。
- 设置 `color-scheme: light`。
- 保留窄屏单列布局和 reduced-motion 适配。

## 12. 持续通信与网速测试

- 支持上行/下行 TCP，最长 300 秒。
- 用 ADB 查找获得 `10.0.1.x` PDU IP 的 USB 手机。
- 手机 `nc` 显式绑定 PDU IP，对端为 `192.168.70.129` 动态端口。
- 流量实际经 5G PDU 会话、UPF 和 external DN，不是 ADB 端口转发。
- 显示瞬时/平均 Mbps、累计字节和时间。
- uplink 可同时作为 PUSCH IQ/LLR 激励。

短信功能未集成：当前没有完整验证 SMS over IMS/VoNR 链路，因此页面不显示不可靠的发短信按钮。

## 13. 日志

- gNB 标签显示 `oai-gnb-runner` 日志。
- Core 标签聚合 10 个 5GC/IMS 容器日志，每行加容器名。
- API 最多返回 500 行；页面每 4 秒请求 180 行。
- 清理 ANSI 颜色字符。

## 14. 验证

```bash
cd '/home/usrp2/桌面/OAI(1)/oai-control-center'
npm test
```

回归测试覆盖页面渲染、服务/频率/增益 API、Scope 二进制解析、LLR、dB 转换、带外显示、白色主题、无 iframe、无未实现短信功能，以及 LAN 监听与远端写保护。

## 15. X310 dBFS/dBm 双单位显示

- 硬件识别为 X310 `312EE64` + CBX，RF1 对应 B 通道 CBX RX2，采集增益为 20 dB。
- UHD 4.8 对两个 CBX RX 通道均返回 `has_rx_power_reference=false`，设备 EEPROM/本机 UHD 数据库中没有可用的绝对功率标定表。
- 保留原始 RMS/peak dBFS，新增已标定的 `rmsDbm`/`peakDbm`。换算式为 `dBm = dBFS + referenceDbm`。
- 未标定时 dBm 显示“未标定”且 API 返回 `null`，不使用最大允许输入功率或经验常数伪造绝对测量值。
- 曾在幅度面板提供“已知输入 RMS” RX 一点标定；后按“TX 频谱仪标定与 RX 采集完全独立”要求从页面移除，仅保留 API/环境变量作为高级 RX 标定通道。
- 新增 `POST /api/x310/calibration`，请求字段为 `knownRmsDbm`，标定文件默认保存到 `backend/x310-power-calibration.json`。
- `/api/status` 的 `radio.monitor.calibration` 返回 `calibrated`、`referenceDbm`、`source`、`rxGainDb`和可选 `updatedAt`。
- 可用 `OAI_MONITOR_RX_REFERENCE_DBM` 直接注入外部已知参考，用 `OAI_MONITOR_RX_GAIN_DB` 修改采集增益。更换频率、增益、子板、端口或线缆后需重新标定。

## 16. X410/X310 连续单音校准模式

- X410 和 X310 设备卡各增加“校准”按钮、实时 TX gain 滑块和单音发射状态。
- 两台设备使用同一个互锁状态机，禁止同时发射两路校准单音。
- X410 启动校准时自动停止 `oai-gnb-runner`，停止校准后恢复原 gNB 运行状态。
- X310 启动校准时，单一 UHD 进程同时在 RF1 `TX/RX` 发射单音、在 RF1 `RX2/RX` 继续采集。采集开关与 TX 标定独立，dBFS/IQ 数据在校准期间仍持续更新。
- `backend/tone_worker.py` 基于 UHD Python `ToneGenerator` 连续发射，并在 X310 模式下同时回传 RX RMS/peak dBFS 和 IQ。通过 stdin 更新模拟 TX gain，拖动滑块时不重建 UHD 流。
- 目标频率取页面当前中心频率。X410 会把请求的 1 MHz 采样率校正为约 0.999 MHz，固定 100 kHz 偏移会令 UHD `ToneGenerator` 报 `Rate/Freq ratio is too odd`。现改为“设备实际采样率 ÷ 8”的相干基带偏移，并同步移动射频 LO，使实际输出仍落在目标频点且避开 DC/LO 泄漏点。
- 数字复数基带样本幅度固定 -20 dBFS；这是送入 DAC 前的数字驱动幅度，不代表 RF 接口功率。RF 端模拟单音功率以频谱仪实测 dBm 为准。X410 gain 为 0–60 dB，X310 CBX gain 为 0–31.5 dB/0.5 dB 步进。
- 新增 `POST /api/rf/calibration-tone`，支持 `start`、`gain`、`stop`、`calibrate`，`/api/status` 增加 `hardware.calibrationTone`。
- TX 标定不使用 X310 采集 RMS：将当前发射端口直接接入频谱仪，页面输入当前负/正 dBm 读数并保存 `frequencyMHz + gainDb + measuredDbm`标定点。
- 修复 dBm 输入框无法键入负号：前端使用字符串保留编辑中的 `-`，仅在提交时转换并验证数值。
- 标定数据保存到 `backend/tx-power-calibration.json`，`/api/status` 增加 `hardware.txCalibration`；同频点多点线性插值，超出范围时外推。
- X410/X310 设备卡新增以 TX gain (dB) 为横轴、频谱仪实测功率 (dBm) 为纵轴的标定折线；空数据时明确显示等待标定点。
- 新增局域网只读 `GET /api/rf/calibration`；可用 `device` 和 `frequencyMHz` 查询参数筛选，响应给出坐标轴语义、当前频点曲线点及设备全部标定点。
- X410/ZBX 有 UHD 名义功率参考；X310/CBX 当前 `has_rx_power_reference=false`。由于隔空天线增益/传播损耗未知，X310 RF 接头处的溯源 dBm 仍需已标定功率计/信号源，或已知衰减的有线标定。
