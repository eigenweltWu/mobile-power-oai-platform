## 一、论文真正要验证的 4 个假设

建议不要把 AC 和 RC 写成两个独立实验，而是围绕四个假设组织。

| 假设   | 要回答的问题                                   | 主要环境     |
| ---- | ---------------------------------------- | -------- |
| H1   | 外部场强/RSRP 变弱时，为什么手机耗电变化？                 | AC       |
| H2   | **相同 RSRP 下，仅改变 gNB 上行功控，手机功耗是否变化？**     | AC       |
| H3   | 相同平均 RSRP 下，LOS 与丰富多径是否产生不同能耗？           | AC vs RC |
| H4   | RSRP-only 模型是否会在 RC 失效，而加入 TPC/PH/MCS/HARQ 后恢复？ | AC→RC    |

其中 **H2 + H4 是整篇论文最重要的结果**。

最终不是证明 $RSRP\downarrow\Rightarrow P_{\rm phone}\uparrow$，而是证明：

$$\boxed{P_{\rm phone}=f(RSRP,\,TPC,\,PH,\,N_{\rm PRB},\,MCS,\,HARQ,\,traffic)}$$

并且这个模型能够跨 AC/RC 环境解释数据。

------

# 二、AC：暗室实验的核心不是方向图，而是“受控因果实验”

暗室固定 DUT 位置和姿态，利用喇叭天线校准 DUT 区域真实入射功率密度 $S_{\rm inc}$，同时改变 gNB TX power，建立：

$$S_{\rm inc}\rightarrow SS\text{-}RSRP\rightarrow \text{gNB/UE behaviour} \rightarrow P_{\rm phone}$$

手机方向图未知并不妨碍这个实验。

------

## AC-0：正式实验前的 RF calibration

固定：

- n78；
- 40 MHz；
- 固定 DUT 距离；
- 固定手机姿态；
- 固定 gNB RX gain；
- 固定 PUSCH target SNR；
- 固定业务模式。

当前系统已经能够记录频率、40 MHz 带宽、TX/RX gain 等配置。

用校准喇叭/功率计建立 $TX\ Gain\rightarrow S_{\rm inc}$，然后放手机进去，再建立 $S_{\rm inc}\rightarrow SS\text{-}RSRP$。

建议选出约 5 个实际稳定工作点，例如 $\text{Strong}\rightarrow\text{Medium}\rightarrow\text{Weak}\rightarrow\text{Near-edge}$，可以初步瞄准 $-65,\,-75,\,-85,\,-95,\,\text{near-threshold dBm}$。

但正式论文使用的应该是**实际测得的 RSRP**，不是这些目标值。

这一步预计得到第一张关键图 $S_{\rm inc}\ \text{vs.}\ RSRP_{\rm phone}$，它首次把“物理入射场”和“手机自己感知的信号强度”联系起来。

------

# 三、AC-1：Signal-strength sweep

这是基础实验。

每个信号等级分别测试：

| Workload      | 目的                 |
| ------------- | ------------------ |
| Idle          | 基础连接功耗             |
| Fixed-rate DL | 看弱信号下接收侧变化         |
| Fixed-rate UL | 看上行 PA/功控变化        |
| Saturated DL  | 看最大吞吐/race-to-idle |
| Saturated UL  | 看 UL power limit   |

其中最重要的是 **Fixed-rate UL**。

因为固定业务量以后，能够避免 $RSRP\uparrow\rightarrow throughput\uparrow\rightarrow CPU/RF工作量\uparrow$ 把结果混在一起。

Fixed-rate 应设为 $R_{\rm fixed}\lesssim 30\%-50\%$ 的“最弱有效信号条件下仍能稳定达到的吞吐量”。

这样每一个 RSRP 条件下，手机都完成相同无线业务负载。

------

## 每一个 AC run 的统一流程

建议固定为 $30s\ baseline\rightarrow120s\ workload\rightarrow60s\ tail$，即：

**Phase 1 — Baseline**：30 s，无主动业务，得到 $P_{\rm idle}$。

**Phase 2 — Active**：120 s UL 或 DL，得到 $P_{\rm active}$、$E_{\rm active}=\int P(t)dt$、$E_{\rm bit}=\frac{E_{\rm active}-E_{\rm idle}}{\text{successfully transferred bits}}$。

**Phase 3 — Tail**：停止业务后继续采集 60 s，得到 $E_{\rm tail}$。

每条件至少重复 3 次，而且条件顺序做 block randomization，防止 $SOC,\ battery\ temperature,\ thermal\ throttling$ 随实验时间单向漂移。

------

# 四、AC-1 预期结果

最可能出现的不是一条简单的 $RSRP\downarrow\Rightarrow P\uparrow$，而是 UL/DL 明显不同。

### Fixed-rate UL

预计 $RSRP\downarrow$，对应 $TPC^{+}\uparrow$、$PH\downarrow$、$P_{\rm UE,UL}\uparrow$，进一步 $P_{\rm phone}\uparrow$。

到非常弱的区域可能出现 $PCMAX\ saturation$，此后即使继续要求正 TPC，$PUSCH\ SNR$ 也无法跟随 target，随后 $BLER\uparrow,\quad HARQ\uparrow,\quad E_{\rm bit}\uparrow$。

这会形成一个非常漂亮的 **energy knee**。

------

### Fixed-rate DL

DL 的变化预计弱得多。

中高 RSRP 区，$P_{\rm phone}$ 可能变化不明显；到了弱覆盖以后，$MCS\downarrow,\quad HARQ\uparrow$，通信持续时间或者接收处理增加，才导致 $E_{\rm bit}\uparrow$。

因此 UL 和 DL 不应该混成一个“5G signal strength vs power”结果。

------

# 五、AC-2：整篇文章最重要的因果实验

现在 OAI 已经能够直接改变：

$$\boxed{PUSCH\ Target\ SNR}$$

当前实现允许 0–40 dB，并已经把 10–25 dB 作为较合理的实验起始区间，同时会同步记录实际 TPC、PH、PCMAX、PUSCH SNR 等。

因此选择两个固定无线条件 $RSRP_{\rm medium}$ 和 $RSRP_{\rm weak}$，保持 $S_{\rm inc},RSRP,姿态,traffic$ 基本不变，仅改变 $PUSCH\ Target\ SNR=10,15,20,25\ {\rm dB}$，运行 Fixed-rate UL。

这样就有：

$$\boxed{RSRP=\mathrm{constant}}$$

但是 $PUSCH\ Target\rightarrow TPC\rightarrow PH\rightarrow P_{\rm phone}$ 发生变化。

------

## AC-2 最理想的结果

例如在 $RSRP\approx-80\,{\rm dBm}$ 附近得到：

$$Target\ SNR\uparrow \rightarrow TPC^{+}\ ratio\uparrow \rightarrow PH_{\rm raw}\downarrow \rightarrow P_{\rm phone}\uparrow$$

而 RSRP 基本不变化，这就直接证明：

$$\boxed{RSRP\ alone\ is\ insufficient}$$

这比任何 RSRP correlation 都强，因为这是**网络侧主动 intervention**。

------

# 六、AC-3：traffic-load sweep

然后只选 $RSRP_{\rm medium}$，做三个 UL offered load：$R_L,\quad R_M,\quad R_H$，目的是把 $N_{\rm PRB}$ 引入。

预计：

$$offered\ load\uparrow \rightarrow N_{\rm PRB}\uparrow \rightarrow P_{\rm phone}\uparrow$$

但 $E_{\rm bit}$ 不一定上升，甚至可能下降。

因此必须同时报告 $P_{\rm avg}$ 和 $E_{\rm bit}$，不能只报告“耗电功率”。

------

# 七、AC-4：转台只作为 robustness，不做天线方向图

可以选四个姿态 $0^\circ,\ 90^\circ,\ 180^\circ,\ 270^\circ$，保持 $S_{\rm inc}=\mathrm{constant}$，观察 $RSRP(\phi)$ 以及 $P_{\rm phone}(\phi)$。

这样可以得到：

> 相同外部入射功率密度，由于手机终端天线/机身/多天线状态不同，手机实际感知的 RSRP 和最终能耗发生变化。

但论文里叫 **orientation sensitivity**，不要叫 **measured antenna radiation pattern**。

进一步最有价值的是对不同角度重新调整 gNB power，使 $RSRP(\phi_1)\approx RSRP(\phi_2)$，再比较能耗。

如果跨姿态的差异在加入 $CQI,RI,PMI,TPC,PH$ 后大幅减小，那么又支持“cross-layer state 比 RSRP 更接近真正的功耗解释变量”。

------

# 八、RC 的定位必须和 AC 完全不同

这里有一个很重要的概念：

**RC 中不要再直接说“与 AC 相同的功率密度”。**

暗室中是明确方向传播 $\vec S_{\rm inc}$，而 RC 是从多个方向、多个极化到达 DUT 的 diffuse multipath field。

因此 AC/RC 比较最干净的 matched variable 应该是：

$$\boxed{SS\text{-}RSRP}$$

而不是强行把两个环境的某个“power density”数值设成一样。

3GPP TS 38.161 把 AC 定义为 FR1 TRP/TRS 的 reference method，但对于 >3 GHz 且不需要方向参数的情况，允许使用 RC 作为替代方法。你们的 n78 工作点正属于 >3 GHz，所以用 RC 做**非方向性的统计无线性能/能耗研究**是合理的；但你们不是在做认证测试，不应该宣称为 3GPP conformance measurement。[ETSI](https://www.etsi.org/deliver/etsi_ts/138100_138199/138161/18.02.00_60/ts_138161v180200p.pdf)

------

# 九、RC-0：先证明这个 RC 确实产生了“不同于 AC 的信道”

不能只写：

> RC is a rich-multipath environment.

需要自己测出来。

现在 OAI 已经有 $SRS\ frequency\ response$ 以及 PUSCH、CSI 等信息。当前平台的 WebScope 已包含上行信道估计。

因此对不同 stirrer state 记录 $H_k(f)$，然后分析 $\sigma_f(|H|)$ 以及 state-to-state variation。

预计：

### AC

$H(f)$ 相对稳定；

### RC

$H_k(f)$ 随 stirrer state 明显变化，而且具有更强的频率选择性。

传统 RC OTA 方法就是利用大量 statistically distributed field combinations；3GPP/ETSI 的 RC TRP 方法甚至要求至少 100 个独立 Rayleigh-faded samples 来建立标准化统计平均。[ETSI](https://www.etsi.org/deliver/etsi_tr/137900_137999/137902/16.00.00_60/tr_137902v160000p.pdf)

做能耗研究不必把“100”当成强制认证要求，但最好先获得足够的独立 stirrer states。

------

# 十、RC-1：AC 和 RC 的真正核心对比

选 AC 中三个 RSRP 条件 $RSRP_S,\quad RSRP_M,\quad RSRP_W$，然后调整 RC 激励，使 $median(RSRP_{RC})\approx RSRP_{AC}$，例如控制在 $\pm1\sim2\,{\rm dB}$ 范围。

运行同一个 $Fixed\text{-}rate\ UL$ 和 $Fixed\text{-}rate\ DL$ 业务。

------

## RC 的采样方式

优先使用 **step stirring**。

每一个 $state_k$ 固定 stirrer，然后 $5s\ settle+10\sim15s\ measurement$。

推荐 $30\sim50$ 个独立状态做功耗统计。

如果后面增加真正的 TRP 测量，则单独使用接近标准 RC 方法的 $N\ge100$ 独立 field states。

不要为了满足 100 个状态，把每个能耗 condition 都乘以 100。

------

# 十一、RC-1 预期出现的核心结果

即使 $median(RSRP_{AC})=median(RSRP_{RC})$，预计 $Var(P_{\rm phone,RC})>Var(P_{\rm phone,AC})$，因为 RC 中 $CQI,\ MCS,\ HARQ,\ TPC,\ PH$ 会随 channel realization 更明显变化。

于是可以出现非常关键的结果：$RSRP=-85\pm1\,{\rm dBm}$ 条件下仍然存在 $P_{\rm phone}=P_1,P_2,\ldots$，而较高的 $HARQ\ retransmission$ 或更强的 $TPC$ 对应更高 $E_{\rm bit}$。

这就是论文第二个核心证据。

RC 被广泛用于模拟各向统计多径及 Rayleigh fading；3GPP 的相关 MIMO OTA 技术报告也明确把 RC 描述为适合模拟 indoor/urban multipath 和 3D signal distribution 的环境。[ETSI](https://www.etsi.org/deliver/etsi_tr/137900_137999/137976/17.00.00_60/tr_137976v170000p.pdf)

------

# 十二、RC-2：把 AC 的 PUSCH intervention 再做一遍

不要在 RC 里重新开一个故事。

选择 $RSRP_M$，然后 $PUSCH\ Target=10,15,20,25\,{\rm dB}$，重复 AC-2。

最后比较 $\frac{\partial P_{\rm phone}}{\partial TargetSNR}$ 在 $AC$ 和 $RC$ 中是否相近。

如果相近，说明：

> gNB closed-loop power-control → UE energy

是一种跨 propagation environment 的机制。

如果不相近，也很有价值，因为可以进一步由 $HARQ,CQI,RI,MCS$ 解释 RC 中的额外差异。

------

# 十三、整篇论文最强的一组模型实验

用 AC 数据训练三个模型。

### Model A — $E_{\rm bit}=f(RSRP)$

### Model B — $E_{\rm bit}=f(RSRP,SINR,traffic)$

### Model C — $E_{\rm bit}=f(RSRP,SINR,TPC,PH,PCMAX,N_{\rm PRB},MCS,HARQ,CQI,RI)$

先做 $AC\ train\rightarrow AC\ holdout$，然后最关键：

$$\boxed{AC\ train\rightarrow RC\ test}$$

预期结果应该是 $Error_A>Error_B>Error_C$，而且 $Error_{A,RC}$ 恶化最明显。

这会产生一个很强的结论：

> **RSRP-based smartphone energy models are environment dependent, whereas cross-layer models using power-control, scheduling, and retransmission states generalize substantially better across LOS and rich-multipath environments.**

------

# 十四、RC 还有一个非常值得增加的可选结果：手机 TRP–电池功耗

这是 RC 最独特的附加价值。

如果完成 $RC\ chamber\ transfer\ calibration$，并且接收链具有绝对功率标定，则可以估计 UL 条件下手机 $P_{\rm TRP}$。

3GPP 当前 FR1 规范允许 >3 GHz 时在不需要方向信息的情况下使用 RC 测非方向性 TRP/TRS；其方法本质也是对多个 statistically independent field states 中的 DUT radiated power 做统计平均。[ETSI](https://www.etsi.org/deliver/etsi_ts/138100_138199/138161/18.02.00_60/ts_138161v180200p.pdf)

这样可以第一次直接建立 $TPC/PH\rightarrow P_{\rm TRP}\rightarrow P_{\rm battery}$，进一步定义 $\eta_{\rm radiated}=\frac{P_{\rm TRP}}{P_{\rm phone}-P_{\rm baseline}}$。

但这个量应叫 **system-level radiated-power efficiency**，不能叫“手机 PA efficiency”，因为分母还包括 modem、baseband、CPU 等功耗。

这一项如果能够做出来，文章档次会明显再提高一层。

------

# 十五、由这些实验反推：手机端 App 应该是什么

现在可以非常明确：

$$\boxed{\text{不要再做一个“手机监控 App”}}$$

需要做的是：

# **5G Energy Experiment Agent**

它实际上是一个：

$$\boxed{\text{Traffic generator}+\text{Phone telemetry logger}+\text{Experiment scheduler}+\text{Time synchronizer}}$$

而 UI 应该极简。

------

# 十六、App 的 6 个核心模块

| 模块               | 必须实现的功能                                  |
| ---------------- | ---------------------------------------- |
| Session Manager  | experiment ID、condition、start/stop、AC/RC metadata |
| Cellular Binder  | 强制实验 socket 走 5G cellular network        |
| Workload Engine  | UL/DL、CBR、fixed-size、saturation、burst    |
| Telemetry Logger | battery + NR radio + thermal             |
| Clock Sync       | 手机与 OAI/实验服务器时间对齐                        |
| Local Recorder   | 屏幕关闭后本地持续记录并最终导出                         |

------

# 十七、第一原则：App 测量时绝对不要去轮询 OAI API

手机端不能每秒执行：

```
GET /api/research/ues
GET /api/research/events
```

因为这本身会创造 $DL\ traffic$ 并改变 $RRC/scheduler/power$。

OAI 数据已经在基站端独立记录。

正确结构应该是 $Phone\ logger$ 和 $OAI\ logger$ 完全并行，最后根据时间 $timestamp$ merge。

------

# 十八、App 必须彻底摆脱 USB/Wi-Fi ADB

当前实验平台原来的持续测速是由 USB 控制手机，但数据本身已经绑定 PDU 地址、经过真实 5G PDU session、UPF 和 external DN。

正式功耗实验最好进一步把**控制链也从 USB 移除**。

App 本身连接实验服务器 $Phone\xrightarrow{5G}UPF\xrightarrow{}External\ DN$。

Android 提供 `ConnectivityManager`/`Network` API，可以请求 cellular transport，并把 TCP 或 UDP socket 明确绑定到对应 `Network`；`Network.bindSocket()` 会使该 socket 的流量明确走指定网络。[Android Developers](https://developer.android.com/reference/android/net/ConnectivityManager?utm_source=chatgpt.com)

因此 $WiFi=OFF$、$USB=disconnected$，手机完全电池供电。

------

# 十九、但是实验过程中也不要保持 WebSocket 心跳

否则 Idle experiment 又被控制流量污染。

最佳方案是：

### 实验开始前

App 与实验服务器做 $clock\ synchronization$，然后服务器下发完整 schedule：

```
experiment_id
condition_id
T0
baseline_duration
workload_type
target_rate
active_duration
tail_duration
```

例如：

```
T0 = 15:30:00
30 s idle
120 s UL 5 Mbps
60 s tail
```

然后手机：

$$\boxed{\text{完全本地执行}}$$

实验窗口内不需要控制包。

完成以后再上传日志。

------

# 二十、App 的时间同步尤其重要

手机每一个 sample 同时记录：

```
utc_epoch_ms
elapsedRealtimeNanos
```

Android 官方文档明确区分二者：wall clock 可能被时间同步调整，而 `elapsedRealtimeNanos()` 是 monotonic clock，包含 deep sleep，适合测量时间间隔。[Android Gooblesource](https://android.googlesource.com/platform/frameworks/base/%2B/HEAD/core/java/android/os/SystemClock.java?utm_source=chatgpt.com)

因此，$UTC$ 负责和 OAI 合并，$elapsedRealtimeNanos$ 负责手机内部精确排序和积分。

实验开始前通过 10–20 次 server ping-pong 求 $\Delta t_{\rm phone-server}$，然后使用最低 RTT 的若干样本估计 clock offset。

------

# 二十一、App 必须采集的 Battery 数据

使用 Android `BatteryManager`：

```
BATTERY_PROPERTY_CURRENT_NOW
BATTERY_PROPERTY_CURRENT_AVERAGE
BATTERY_PROPERTY_CHARGE_COUNTER
```

以及：

```
voltage
temperature
SOC
charging state
plugged state
```

Android 官方 API 中 `CURRENT_NOW` 是瞬时电池电流、`CURRENT_AVERAGE` 是硬件定义时间窗口上的平均电流、`CHARGE_COUNTER` 是剩余电荷；具体平均窗口取决于手机 fuel-gauge 硬件。[Android Developers](https://developer.android.com/reference/android/os/BatteryManager?utm_source=chatgpt.com)

因此手机 App 建议原始采样 $5\ {\rm Hz}$，然后实际分析统一聚合到 $1s$ 或 $5s$ 窗口。

不要试图声称：

> 一个 slot-level TPC event 导致了 5 ms 后多少 mA 的变化。

手机 battery fuel gauge 的时间响应没有这么强。

正确方法是让 $\text{1 s 内 TPC+/HARQ/NPRB statistics}$ 和 $\text{1 s average battery power}$ 相关。

------

# 二十二、App 应记录的手机 NR 数据

Android `CellSignalStrengthNr` 从 API 29 开始可直接提供 $SS\text{-}RSRP$、$SS\text{-}RSRQ$、$SS\text{-}SINR$，以及在设备支持时 $CSI\text{-}RSRP,\,CSI\text{-}RSRQ,\,CSI\text{-}SINR$。

API 31 以后还提供 CSI CQI report，但具体 modem 是否上报仍可能返回 unavailable。[Android Developers](https://developer.android.com/reference/kotlin/android/telephony/CellSignalStrengthNr?utm_source=chatgpt.com)

因此 App 保存：

```
phone_ss_rsrp_dbm
phone_ss_rsrq_db
phone_ss_sinr_db

phone_csi_rsrp_dbm
phone_csi_rsrq_db
phone_csi_sinr_db
```

不可用就：

```
null
```

不要依赖手机端 CQI。

因为 OAI 端已经有更完整 $CQI,RI,PMI,UL-RI,TPMI$，手机 App 的无线指标主要用于记录**手机自己看到的信号**。

同时记录：

```
NRARFCN
PCI
NCI
TAC
```

用来证明整个实验中 serving cell 没发生变化。

------

# 二十三、App 必须具有自己的流量引擎

至少需要 6 种工作模式：

| Mode               | 用途                         |
| ------------------ | -------------------------- |
| `IDLE`             | baseline/tail              |
| `UL_CBR`           | 最重要，固定业务量研究 UL power       |
| `DL_CBR`           | 固定 DL workload             |
| `UL_FIXED_SIZE`    | task energy                |
| `DL_FIXED_SIZE`    | task energy / race-to-idle |
| `UL/DL_SATURATION` | 最大性能上限                     |
| `BURST`            | 后续 tail/RRC 实验             |

CBR 优先 UDP，因为 $offered\ load$ 可以直接设定。TCP 用于 $fixed\ file$ 和 $saturation$ 实验。

App 自己记录真实：

```
payload bytes
socket bytes
duration
goodput
packet count
```

Android `TrafficStats` 还可以读取该 App UID 的 TX/RX bytes/packets，作为第二套流量计数核验。[Android Developers](https://developer.android.com/reference/android/net/TrafficStats?utm_source=chatgpt.com)

------

# 二十四、App 还必须记录 thermal state

手机热效应会成为非常大的 confounder。

因此同时记录：

```
battery_temperature
thermal_status
```

Android 从 API 29 提供系统 thermal status listener。[Android Developers](https://developer.android.com/reference/android/os/PowerManager.OnThermalStatusChangedListener?utm_source=chatgpt.com)

如果实验过程中 $thermal\ status$ 变化，或者 $T_{\rm battery}$ 出现明显持续漂移，那么该 run 应单独标记。

这比后期发现：

> 为什么第 20 组永远比第 1 组耗电低/高？

再去猜原因可靠得多。

------

# 二十五、App 每条数据最终建议长这样

```
timestamp_utc_ms
elapsed_realtime_ns

experiment_id
condition_id
phase
workload_type

battery_current_ua
battery_current_average_ua
battery_voltage_mv
battery_charge_counter_uah
battery_soc
battery_temperature_c
thermal_status
plugged

ss_rsrp_dbm
ss_rsrq_db
ss_sinr_db

csi_rsrp_dbm
csi_rsrq_db
csi_sinr_db

nrarfcn
pci
nci
tac

cellular_network_id

app_tx_bytes
app_rx_bytes

workload_target_mbps
workload_actual_mbps
```

------

# 二十六、OAI 和手机最后怎么合并

OAI 已经有 $timestampUtc$、$timestampEpochNs$、$timestampMonotonicNs$ 用于研究同步。README.mdMD

最终按 1 s window 得到：

$$\begin{aligned}X_t=[&S_{\rm inc},\\&RSRP_{\rm phone},RSRP_{\rm OAI},\\&SS\text{-}SINR,PUSCH\ SNR,\\&TPC^{+/-},PH_{\rm raw},PH_{\rm norm},PCMAX,\\&N_{\rm PRB},MCS,CQI,RI,\\&BLER,HARQ,\\&goodput]\end{aligned}$$

对应 $Y_t=[P_{\rm phone},E_{\rm bit}]$，这才是最后进入模型的数据集。

------

# 二十七、最终 App 的 UI 反而应该非常简单

主页只需要：

```
DEVICE
5G SA connected
Cellular network bound
Battery only
Server reachable

EXPERIMENT
Experiment ID
Condition ID
Workload
Duration

TELEMETRY
Current
Voltage
Temperature
RSRP

[ARM EXPERIMENT]
[STOP]
```

点击 **ARM** 以后，$10s\ countdown$，然后关闭 UI、进入 foreground measurement service。

正式测量窗口：

$$\boxed{\text{screen OFF}}$$

不实时画曲线，不刷新复杂页面，不运行 GNSS，不保持 Wi-Fi，不进行 OAI polling。

原来的网页继续作为**实验员端 dashboard**。

手机 App 只做**实验执行器**。

------

# 二十八、论文最终最理想的 6 张结果图也已经基本确定

| Figure | 应产生的结果                                   |
| ------ | ---------------------------------------- |
| Fig. 1 | AC + RC + OAI + phone cross-layer testbed |
| Fig. 2 | calibrated $S_{\rm inc}$ → phone RSRP → battery energy |
| Fig. 3 | UL/DL energy vs RSRP + TPC/PH/HARQ mechanism |
| Fig. 4 | **fixed RSRP + PUSCH target SNR intervention** |
| Fig. 5 | matched-RSRP AC vs RC energy distributions |
| Fig. 6 | RSRP-only vs cross-layer model：AC→AC 与 AC→RC |

如果 RC 再完成 TRP，**Fig. 5 或单独 Fig. 7** 可以做 $P_{\rm TRP}\ \text{vs}\ P_{\rm phone}$。

------

## 最终实验主线

整篇文章最终可以压缩成：

$$\boxed{\textbf{AC}}$$

建立 $S_{\rm inc}\rightarrow RSRP\rightarrow TPC/PH/N_{\rm PRB}/HARQ\rightarrow Energy$，并通过 $PUSCH\ Target\ SNR$ 完成主动因果验证。

然后：

$$\boxed{\textbf{RC}}$$

在相同 RSRP 下引入 statistically rich multipath，检验 $RSRP\text{-only model}$ 是否发生 domain failure。

最后证明：

$$\boxed{\text{cross-layer model}}$$

能够明显更好地解释和预测 AC + RC 中的手机能耗。

因此手机端需要开发的不是当前监控网页的 Android 复刻版，而是一个**无 USB、无 Wi-Fi、蜂窝 socket 强制绑定、屏幕关闭运行、可生成严格 UL/DL workload、同步记录 battery/NR/thermal telemetry，并能够按照预下载 schedule 独立执行实验的 Android 5G Energy Experiment Agent**。
